<!DOCTYPE html>
<html lang="ptbr">

<head>
    <meta charset="UTF-8">
    <title>Sobre a Exposição virtual de fotgrafias Astronômicas</title>
    <link rel="stylesheet" href="styles3.css">
    <style>
        body {
            margin: 0;
            overflow: hidden;
            font-family: 'Press Start 2P', cursive;
            color: white;
        }

        .background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .shooting-star {
            position: absolute;
            width: 1px;
            height: 5px;
            background-color: white;
            opacity: 0;
            animation: shootingStar 5s linear infinite;
        }

        @keyframes shootingStar {
            from {
                opacity: 1;
                transform: translateY(0) rotate(45deg);
            }
            to {
                opacity: 0;
                transform: translateY(100vh) rotate(45deg);
            }
        }
    </style>
</head>

<body>
    <!-- Restante do seu conteúdo -->

    <!-- Fundo de Meteoro -->
    <div class="background"></div>

    <footer>
        <p>&copy; 2023 Projeto Espaco Ciencias</p>
    </footer>
</body>

</html>

body {
    margin: 0;
    overflow: hidden;
    font-family: 'Press Start 2P', cursive;
    color: white;
}

.background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: -1;
    background: radial-gradient(rgba(255, 255, 255, 0.1) 1%, transparent 1%);
    background-size: 3px 3px;
}

.shooting-star {
    position: absolute;
    width: 1px;
    height: 5px;
    background-color: white;
    opacity: 0;
    animation: shootingStar 5s linear infinite;
}

@keyframes shootingStar {
    from {
        opacity: 1;
        transform: translateY(0) rotate(45deg);
    }
    to {
        opacity: 0;
        transform: translateY(100vh) rotate(45deg);
    }
}

